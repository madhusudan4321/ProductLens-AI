[project]
name = "productlens-ai-service"
version = "1.0.0"
description = "ProductLens AI — AI-powered product intelligence research service"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
