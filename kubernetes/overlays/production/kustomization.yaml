# ProductLens AI — Production Overlay
# Placeholder — will be fully configured in Phase 10
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: productlens

resources:
  - ../../base/namespace.yaml
  - ../../base/frontend/deployment.yaml
  - ../../base/backend/deployment.yaml
  - ../../base/ai-service/deployment.yaml
  - ../../base/workers/deployment.yaml

# Patches, configMapGenerator, secretGenerator to be added in Phase 10
